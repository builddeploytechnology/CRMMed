apiVersion: v1
kind: Service

metadata:
  name: crm-frontend-service

spec:
  type: NodePort

  selector:
    app: crm-frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5173
      nodePort: 30080
